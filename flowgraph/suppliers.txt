================================================================================
                   WHITEBOX TESTING - FLOWGRAPH ANALYSIS
                   FUNGSI: handleSubmit (Suppliers Page)
================================================================================

KODE YANG DIUJI (Source Code):
===============================

const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    if (offCanvasMode === 'add' && !checkActionPermission('create')) {
        goeyToast.error('Akses Ditolak', { description: "..." }); return;
    }
    if (offCanvasMode === 'edit' && !checkActionPermission('edit')) {
        goeyToast.error('Akses Ditolak', { description: "..." }); return;
    }
    try {
        const url = offCanvasMode === 'add' ? '.../api/suppliers' : `.../api/suppliers/${selectedSupplier?.id}`;
        const method = offCanvasMode === 'add' ? 'POST' : 'PUT';
        const res = await fetch(url, { method, headers: {...}, body: JSON.stringify(formData) });
        if (res.ok) { setIsOffCanvasOpen(false); fetchSuppliers(); goeyToast.success(...); }
        else { goeyToast.error(...); }
    } catch (error) { console.error(error); goeyToast.error(...); }
};


1. PENJELASAN NODE
==================
Node 1  : e.preventDefault() - Mencegah reload form
Node 2  : Cek: mode add && !permission create
Node 3  : Jika true: toast error, proses dihentikan
Node 4  : Cek: mode edit && !permission edit
Node 5  : Jika true: toast error, proses dihentikan
Node 6  : try { - Mulai blok try
Node 7  : Tentukan url dan method (POST/PUT)
Node 8  : Fetch ke API
Node 9  : Cek apakah res.ok
Node 10 : Jika ok: close offcanvas, fetch ulang, toast sukses
Node 11 : Jika gagal: toast error
Node 12 : catch: console.error, toast error
Node 13 : Selesai


2. FLOWGRAPH
=============
1 -> 2
2 -> 3 (T) -> 13
2 -> 4 (F)
4 -> 5 (T) -> 13
4 -> 6 (F)
6 -> 7 -> 8 -> 9
9 -> 10 (T) -> 13
9 -> 11 (F) -> 13
6 -> 12 -> 13


3. GAMBAR FLOWGRAPH (VISUAL)
=============================
    ┌─────┐
    │  1  │
    └──┬──┘
       │
    ┌──▼──┐
    │  2  │
    └──┬──┘
       │
   ┌───┴───┐
   │ T     │ F
   │       │
┌──▼──┐ ┌──▼──┐
│  3  │ │  4  │
└──┬──┘ └──┬──┘
   │       │
   │   ┌───┴───┐
   │   │ T     │ F
   │   │       │
   │┌──▼──┐ ┌──▼──┐
   ││  5  │ │  6  │
   │└──┬──┘ └──┬──┘
   │   │       │
   │   │   ┌──▼──┐
   │   │   │  7  │
   │   │   └──┬──┘
   │   │      │
   │   │   ┌──▼──┐
   │   │   │  8  │
   │   │   └──┬──┘
   │   │      │
   │   │   ┌──▼──┐
   │   │   │  9  │
   │   │   └──┬──┘
   │   │      │
   │   │  ┌───┴───┐
   │   │  │ T     │ F
   │   │  │       │
   │   │┌─▼──┐ ┌─▼──┐
   │   ││ 10 │ │ 11 │
   │   │└─┬──┘ └─┬──┘
   │   │  │      │
   │   │  └──┬───┘
   │   │     │
   │   │  ┌──▼──┐
   │   │  │ 12  │
   │   │  └──┬──┘
   │   │     │
   │   └─────┼─────┘
   └─────────┘
             │
         ┌───▼───┐
         │  13   │
         └───────┘


4. MENGHITUNG CC (CYCLOMATIC COMPLEXITY)
==========================================
Rumus 1: V(G) = E - N + 2
E = 16, N = 13
V(G) = 16 - 13 + 2 = 5

Rumus 2: V(G) = P + 1
Predicate Nodes: 2, 4, 9 = 3
V(G) = 3 + 1 = 4

Rumus 3: V(G) = R = 4

V(G) = 4


5. IDENTIFIKASI PATH
=====================
CC = 4, terdapat 4 jalur independen:

PATH 1: Tidak punya izin create
1 -> 2 -> 3 -> 13

PATH 2: Tidak punya izin edit
1 -> 2 -> 4 -> 5 -> 13

PATH 3: Simpan supplier sukses
1 -> 2 -> 4 -> 6 -> 7 -> 8 -> 9 -> 10 -> 13

PATH 4: Simpan supplier gagal / catch error
1 -> 2 -> 4 -> 6 -> 7 -> 8 -> 9 -> 11 -> 13


6. RINGKASAN
=============
N (Node)             : 13
E (Edge)             : 16
P (Predicate Node)   : 3
V(G) = E - N + 2     : 5
V(G) = P + 1         : 4
Jumlah Path          : 4
Interpretasi         : RENDAH (<=10) - Mudah di-test
